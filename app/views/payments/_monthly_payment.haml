- students.each do |student|
  %tr
    %td= student.id
    %td= student.first_name
    %td= student.last_name
    %td= payment.decorate.check_payment_month(student)
    %td= payment.decorate.find_months(student)
    %td= payment.decorate.find_date(student)
